const {authorized,configured,sameOrigin,settings}=require('../lib/store');
module.exports=async(req,res)=>{res.setHeader('Cache-Control','no-store');if(req.method!=='POST')return res.status(405).json({error:'Method not allowed'});if(!sameOrigin(req)||!authorized(req))return res.status(401).json({error:'Invalid password, or ADMIN_PASSWORD is not configured (minimum 16 characters).'});if(!configured())return res.status(503).json({error:'Connect Google Sheets before editing settings. See the setup guide.'});try{return res.status(200).json(await settings());}catch{return res.status(503).json({error:'Cannot read the Settings tab. Check sheet access and configuration.'});}};
